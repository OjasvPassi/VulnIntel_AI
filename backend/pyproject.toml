[tool.poetry]
name = "vulnintel-backend"
version = "0.1.0"
description = "VulnIntel AI backend (foundations)"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"

[tool.poetry.dev-dependencies]
black = "^24.0"
ruff = "^0.12.0"
isort = "^5.12.0"
pytest = "^8.2.0"
pre-commit = "^3.0"
